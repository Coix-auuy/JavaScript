1.简单数据类型
    值类型：简单数据类型放到栈里面，在存储时变量存储的是值本身。
    string number boolean undefined null-返回的是一个空的对象
    如果有个变量我们以后打算存储为对象，暂时没想好放啥，这个时候就给null。
2.复杂数据类型
    引用类型：复杂数据类型放到堆里面，在存储时变量(栈)中存储的仅仅是地址（引用）。
    通过new关键字创建的对象。
3.简单数据类型传参
    形参和实参不会相互影响。
4.复杂数据类型传参
    传递的是引用。